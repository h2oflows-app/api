-- Irreversible: the auto-forked copies are hard-deleted and not recoverable
-- from this migration. (They were never original content — just copies of
-- @h2oflows runs, which remain intact.) No-op.
SELECT 1;
